type Cat = {
    ears: string | number,
    face: string | number,
    tail: string | number, 
    body: string | number,
    type: string | number,
    colour: string | number,
    id: string | number,
}